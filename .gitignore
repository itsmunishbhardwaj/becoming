node_modules
dist
.DS_Store
*.local
.claude/settings.local.json
.gstack/
vault/logs/
coverage/

# LLM API keys — never commit real env files
.env
.env.*
!.env.example

# VSCode local-history plugin — never commit (may contain secret snapshots)
.history/

# Obsidian vault-local config (per-user)
.obsidian/

# Vault lives outside the repo (set VAULT_ROOT in .env)
# In-repo vault/ is a dev fallback only — never commit user data.
vault/

